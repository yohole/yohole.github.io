language: node_js # 设置语言 
node_js: stable # 设置相应版本
install:
    - npm install # 安装hexo及插件
script:
    - hexo clean # 清除
    - hexo g # 生成
after_script:
    - cd ./public
    - git init
    - git config user.name "Yohole Huang" 
    - git config user.email "490767385@qq.com" 
    - git add .
    - git commit -m "Travis CI Auto Builder"
    - git push --force --quiet "https://${GH_TOKEN_GITHUB}@${GH_REF_GITHUB}" master:master
    - git push --force --quiet "https://${GH_TOKEN_GITEE}@${GH_REF_GITEE}" master:master 
branches:
    only:
        - hexo 
env:
    global:
        - GH_REF_GITHUB: github.com/yohole/yohole.github.io.git
        - GH_REF_GITEE: gitee.com/490767385/yohole.gitee.com.git
